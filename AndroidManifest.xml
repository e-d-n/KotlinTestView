<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.androidstackoverflow.kotlintestview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EnterParentActivity" />
        <activity android:name=".ViewParentActivity" />
        <activity android:name=".EnterChildActivity" />
        <activity android:name=".ViewChildActivity" />
        <activity android:name=".ViewAllActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".TVActivity" />
    </application>

</manifest>